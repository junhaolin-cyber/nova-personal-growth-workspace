import { Mic, Send, Square } from "lucide-react";

export function SpeakingInput({ value, onChange, onSend, onVoice, voiceStatus, voiceError, disabled }: { value: string; onChange: (value: string) => void; onSend: () => void; onVoice: () => void; voiceStatus: "idle" | "listening" | "error"; voiceError: string; disabled: boolean }) {
  const canSend = value.trim().length > 0 && !disabled;
  return <div className="border-t border-line bg-white p-4 sm:p-5"><div className="flex items-end gap-2 rounded-2xl border border-line bg-canvas p-2 focus-within:border-[#8D8BEF] focus-within:ring-4 focus-within:ring-[#5E5CE6]/10"><textarea value={value} onChange={(event) => onChange(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); if (canSend) onSend(); } }} disabled={disabled} rows={2} placeholder="输入英文回答，按 Enter 发送，Shift + Enter 换行" className="min-h-[52px] flex-1 resize-none bg-transparent px-2 py-2 text-sm outline-none placeholder:text-muted/70" /><button type="button" onClick={onVoice} disabled={disabled} className={`grid size-10 shrink-0 place-items-center rounded-xl transition ${voiceStatus === "listening" ? "bg-[#F7E5D5] text-[#B26F3C]" : "bg-white text-muted hover:text-accent"}`} aria-label={voiceStatus === "listening" ? "停止语音输入" : "开始语音输入"}>{voiceStatus === "listening" ? <Square size={16} fill="currentColor" /> : <Mic size={18} />}</button><button type="button" onClick={onSend} disabled={!canSend} className="grid size-10 shrink-0 place-items-center rounded-xl bg-ink text-white transition enabled:hover:-translate-y-0.5 disabled:cursor-not-allowed disabled:opacity-35" aria-label="发送回答"><Send size={17} /></button></div>{voiceError && <p className="mt-2 text-xs text-[#B26F3C]">{voiceError}</p>}<p className="mt-2 text-[11px] text-muted">语音识别结果会先填入输入框，你确认后再发送。</p></div>;
}

